services:
  - type: worker
    name: telegram-content-agent
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python agent.py
    envVars:
      - key: BOT_TOKEN
        sync: false
      - key: CHANNEL_ID
        value: "@HoshMasnoeiAI6"
